---
import BaseLayout from "./BaseLayout.astro";
import CartIcon from "../components/islands/CartIcon";
import CartSlideOver from "../components/islands/CartSlideOver";
import UserMenu from "../components/islands/UserMenu";
import NewsletterPopup from "../components/NewsletterPopup.astro";
import PageAnimations from "../components/islands/PageAnimations";
import { getCategories } from "../lib/supabase";

export interface Props {
    title: string;
    description?: string;
}

const { title, description } = Astro.props;

// Fetch categories
let categories: { id: string; name: string; slug: string }[] = [];

try {
    categories = await getCategories();
} catch (e) {
    console.error("Error fetching categories:", e);
}
---

<BaseLayout title={title} description={description}>
    <!-- Header - Glassmorphism -->
    <header
        class="sticky top-0 z-30 bg-dark-500/80 backdrop-blur-xl border-b border-white/5"
    >
        <div class="container-custom">
            <div class="flex items-center justify-between h-16 md:h-20 gap-4">
                <!-- Logo -->
                <a href="/" class="flex items-center flex-shrink-0">
                    <img
                        src="/logo/logo.svg?v=2"
                        alt="FashionMarket"
                        class="h-8 md:h-10 w-auto"
                        loading="eager"
                    />
                </a>

                <!-- Desktop Navigation - Direct Categories -->
                <nav class="hidden lg:flex items-center gap-1">
                    {
                        categories.map((category) => (
                            <a
                                href={`/productos?categoria=${category.slug}`}
                                class="px-3 py-2 text-zinc-300 hover:text-white font-medium transition-colors text-sm"
                            >
                                {category.name}
                            </a>
                        ))
                    }

                    <!-- Ofertas Link -->
                    <a
                        href="/ofertas"
                        class="px-3 py-2 text-neon-fuchsia hover:text-neon-fuchsia-light transition-colors font-medium flex items-center gap-1 text-sm"
                    >
                        <span
                            class="w-1.5 h-1.5 rounded-full bg-neon-fuchsia animate-pulse"
                        ></span>
                        Ofertas
                    </a>
                </nav>

                <!-- Actions -->
                <div class="flex items-center gap-3">
                    <!-- User Menu -->
                    <UserMenu client:load />

                    <!-- Cart Button -->
                    <div id="cart-trigger" class="relative">
                        <CartIcon client:load />
                    </div>

                    <!-- Mobile Menu Button -->
                    <button
                        id="mobile-menu-btn"
                        class="lg:hidden p-2 text-zinc-400 hover:text-white transition-colors"
                        aria-label="Abrir menú"
                    >
                        <svg
                            class="w-6 h-6"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="1.5"
                                d="M4 6h16M4 12h16M4 18h16"></path>
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Navigation -->
        <nav
            id="mobile-nav"
            class="hidden lg:hidden bg-dark-400/95 backdrop-blur-xl border-t border-white/5"
        >
            <div class="container-custom py-4 space-y-1">
                <!-- User Section Mobile -->
                <div class="pb-3 mb-3 border-b border-white/10">
                    <a
                        href="/auth/login"
                        class="flex items-center gap-3 py-3 px-4 text-zinc-300 hover:text-white hover:bg-white/5 rounded-lg transition-colors font-medium"
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="1.5"
                                d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                            ></path>
                        </svg>
                        Mi Cuenta
                    </a>
                </div>

                <a
                    href="/productos"
                    class="block py-3 px-4 text-zinc-300 hover:text-white hover:bg-white/5 rounded-lg transition-colors font-medium"
                >
                    Todos los Productos
                </a>
                {
                    categories.map((cat) => (
                        <a
                            href={`/productos?categoria=${cat.slug}`}
                            class="block py-3 px-4 text-zinc-400 hover:text-white hover:bg-white/5 rounded-lg transition-colors"
                        >
                            {cat.name}
                        </a>
                    ))
                }
                <a
                    href="/productos?ofertas=true"
                    class="block py-3 px-4 text-neon-fuchsia hover:bg-neon-fuchsia/10 rounded-lg transition-colors font-medium"
                >
                    Ofertas
                </a>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="flex-1">
        <slot />
    </main>

    <!-- Footer - Dark with Gradient Accent -->
    <footer class="bg-dark-600 border-t border-white/5 mt-auto">
        <!-- Gradient Line -->
        <div
            class="h-px bg-gradient-to-r from-transparent via-neon-cyan/50 to-transparent"
        >
        </div>

        <div class="container-custom py-12">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8 md:gap-12">
                <!-- Brand -->
                <div>
                    <div class="mb-4">
                        <img
                            src="/logo/logo.svg"
                            alt="FashionMarket"
                            class="h-9 w-auto"
                            loading="lazy"
                        />
                    </div>
                    <p class="text-zinc-500 text-sm leading-relaxed mb-4">
                        Moda masculina premium. Diseño futurista y elegancia
                        sofisticada para el hombre moderno.
                    </p>

                    <!-- Social Icons -->
                    <div class="flex gap-3">
                        <a
                            href="#"
                            class="w-9 h-9 rounded-full bg-white/5 flex items-center justify-center text-zinc-400 hover:text-neon-cyan hover:bg-neon-cyan/10 transition-all"
                        >
                            <svg
                                class="w-4 h-4"
                                fill="currentColor"
                                viewBox="0 0 24 24"
                                ><path
                                    d="M24 4.557c-.883.392-1.832.656-2.828.775 1.017-.609 1.798-1.574 2.165-2.724-.951.564-2.005.974-3.127 1.195-.897-.957-2.178-1.555-3.594-1.555-3.179 0-5.515 2.966-4.797 6.045-4.091-.205-7.719-2.165-10.148-5.144-1.29 2.213-.669 5.108 1.523 6.574-.806-.026-1.566-.247-2.229-.616-.054 2.281 1.581 4.415 3.949 4.89-.693.188-1.452.232-2.224.084.626 1.956 2.444 3.379 4.6 3.419-2.07 1.623-4.678 2.348-7.29 2.04 2.179 1.397 4.768 2.212 7.548 2.212 9.142 0 14.307-7.721 13.995-14.646.962-.695 1.797-1.562 2.457-2.549z"
                                ></path></svg
                            >
                        </a>
                        <a
                            href="#"
                            class="w-9 h-9 rounded-full bg-white/5 flex items-center justify-center text-zinc-400 hover:text-neon-fuchsia hover:bg-neon-fuchsia/10 transition-all"
                        >
                            <svg
                                class="w-4 h-4"
                                fill="currentColor"
                                viewBox="0 0 24 24"
                                ><path
                                    d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"
                                ></path></svg
                            >
                        </a>
                    </div>
                </div>

                <!-- Links -->
                <div>
                    <h4 class="font-display text-white text-lg mb-4">Tienda</h4>
                    <ul
                        class="grid grid-cols-2 gap-x-4 gap-y-2 text-sm text-zinc-500"
                    >
                        <li>
                            <a
                                href="/productos"
                                class="hover:text-neon-cyan transition-colors"
                                >Productos</a
                            >
                        </li>
                        {
                            categories.slice(0, 6).map((cat) => (
                                <li>
                                    <a
                                        href={`/productos?categoria=${cat.slug}`}
                                        class="hover:text-neon-cyan transition-colors"
                                    >
                                        {cat.name}
                                    </a>
                                </li>
                            ))
                        }
                        <li>
                            <a
                                href="/productos?ofertas=true"
                                class="hover:text-neon-fuchsia transition-colors"
                                >Ofertas</a
                            >
                        </li>
                    </ul>
                </div>

                <!-- Contact -->
                <div>
                    <h4 class="font-display text-white text-lg mb-4">
                        Contacto
                    </h4>
                    <ul class="space-y-3 text-sm text-zinc-500">
                        <li class="flex items-center gap-2">
                            <svg
                                class="w-4 h-4 text-neon-cyan flex-shrink-0"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                                ></path>
                            </svg>
                            noreply@roomieapp.info
                        </li>
                        <li class="flex items-center gap-2">
                            <svg
                                class="w-4 h-4 text-neon-cyan flex-shrink-0"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"
                                ></path>
                            </svg>
                            +34 900 123 456
                        </li>
                    </ul>
                </div>
            </div>

            <div
                class="border-t border-white/5 mt-8 pt-6 flex flex-col md:flex-row items-center justify-between gap-4 text-zinc-600 text-sm"
            >
                <span
                    >© {new Date().getFullYear()} FashionMarket. Todos los derechos
                    reservados.</span
                >
                <div class="flex gap-6">
                    <a href="/privacidad" class="hover:text-zinc-400 transition-colors"
                        >Privacidad</a
                    >
                    <a href="/terminos" class="hover:text-zinc-400 transition-colors"
                        >Términos</a
                    >
                </div>
            </div>
        </div>
    </footer>

    <!-- Cart Slide Over (React Island) -->
    <div id="cart-slideover">
        <CartSlideOver client:load />
    </div>

    <!-- Newsletter Popup -->
    <NewsletterPopup />

    <!-- Page Animations (GSAP) - Isolated component -->
    <PageAnimations client:load />

    <!-- Scripts -->

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            // Cart toggle
            const cartTrigger = document.getElementById("cart-trigger");
            cartTrigger?.addEventListener("click", () => {
                window.dispatchEvent(new CustomEvent("toggle-cart"));
            });

            // Mobile menu toggle
            const mobileBtn = document.getElementById("mobile-menu-btn");
            const mobileNav = document.getElementById("mobile-nav");
            mobileBtn?.addEventListener("click", () => {
                mobileNav?.classList.toggle("hidden");
            });

            // Mobile search toggle
            const mobileSearchBtn =
                document.getElementById("mobile-search-btn");
            const mobileSearch = document.getElementById("mobile-search");
            mobileSearchBtn?.addEventListener("click", () => {
                mobileSearch?.classList.toggle("hidden");
            });

            // Search tags - fill search input and redirect
            const searchTags = document.querySelectorAll(".search-tag");
            const searchInput = document.getElementById(
                "search-input",
            ) as HTMLInputElement;

            searchTags.forEach((tag) => {
                tag.addEventListener("click", () => {
                    const searchTerm =
                        (tag as HTMLElement).dataset.search || "";
                    if (searchInput) {
                        searchInput.value = searchTerm;
                    }
                    // Redirect to products with search
                    window.location.href = `/productos?buscar=${encodeURIComponent(searchTerm)}`;
                });
            });
        });
    </script>
</BaseLayout>
